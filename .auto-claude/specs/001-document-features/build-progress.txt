# Build Progress: 001-document-features

## subtask-1-1: Audit server-side features
**Status:** COMPLETE

### Server Feature Audit Summary

All 19 files in server/src/ audited. Every file is fully implemented (no stubs/partials).

**Core Infrastructure (3 files):**
- index.ts — Fastify bootstrap, route registration, health check, SPA fallback, Telegram bot startup
- db.ts — PostgreSQL connection pool (DATABASE_URL)
- ws.ts — WebSocket real-time broadcast with per-user visibility filtering

**Authentication (2 files):**
- auth.ts — Argon2id hashing, session management (30-day), mirror token auth, requireUser/requireUserOrMirror handlers
- routes/auth.ts — Register, login, logout, GET /me, PATCH /me, GET /users (6 endpoints)

**Card Management (2 files):**
- cards.ts — Card types, visibility predicate (creator/assigned/shared/unassigned), scope filtering (personal/inbox/all), activity logging
- routes/cards.ts — CRUD + scope filtering + drag-drop positioning + WebSocket broadcast (5 endpoints)

**Mirror/Kiosk (1 file):**
- routes/mirror.ts — Create/list/delete mirror tokens for read-only /my-day access (3 endpoints)

**Review (1 file):**
- routes/review.ts — Weekly review: done/stale/stuck cards + AI summary (1 endpoint)

**Telegram Integration (3 files):**
- routes/telegram.ts — Webhook endpoint, user linking, identity CRUD (4 endpoints)
- telegram/bot.ts — Full message handling: text (with AI proposals), voice (Whisper), photos (vision), commands (/today, /assign, /share), inline button callbacks (save/edit/link/drop/move/archive/privacy)
- telegram/proposals.ts — In-memory pending proposal cache with 10-min TTL

**AI Integration (5 files):**
- ai/openai.ts — Dual provider (OpenRouter primary + OpenAI fallback), lazy init, configurable models
- ai/propose.ts — Text → card proposal via LLM (actionability classification, title/desc/tags)
- ai/vision.ts — Image → card via vision model
- ai/whisper.ts — Audio → text via OpenAI Whisper
- ai/weekly_summary.ts — AI weekly review paragraph (done/stale/stuck)

**Attachments (1 file):**
- routes/attachments.ts — Authenticated static file serving for voice/image attachments

**Tests (1 file):**
- __tests__/telegram_parse.test.ts — 8 tests for extractHashtags, parseCommand, extractMentions
